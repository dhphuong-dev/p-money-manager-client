export enum ELocalStorage {
  APP = 'p-money-manager',
  ACCESS_TOKEN = ELocalStorage.APP + '-access-token',
  DARK_MODE = ELocalStorage.APP + '-dark-mode',
  SETTINGS = ELocalStorage.APP + '-settings',
  TRANSACTION_FILTER = ELocalStorage.APP + '-transaction-filter'
}
